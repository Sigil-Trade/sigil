# Phase 9 — Dead-Export Audit

**Phase 9 Batch G — ISC-91.**
**Status:** AUDIT ONLY. No `@deprecated` tags applied in Phase 9 per user
decision D-6 (2026-05-20). All 54 listed exports remain published from
`@usesigil/kit@0.16.0`; deprecation tagging deferred to a v0.17 prep
phase.

## Method

Following the H-SDK10 audit framing from 2026-05-19, this audit
enumerates every root-barrel re-export from `sdk/kit/src/index.ts` that
has no observed internal consumer (grep returns zero non-barrel call
sites inside `agent-middleware/`). External `sigil-dashboard/`
consumption is opaque to this audit; the v0.17 prep phase will rerun
the analysis with explicit dashboard-side coverage before any deletion.

**Pre-tag template for v0.17 prep** (NOT applied in this phase):

```typescript
/**
 * @deprecated Will be removed in @usesigil/kit@0.17.0 (target: Q3 2026).
 * @see {newSymbol} — direct replacement.
 *
 * Migration:
 *   - Old: import { OldSymbol } from "@usesigil/kit"
 *   - New: import { newSymbol } from "@usesigil/kit/subpath"
 */
```

## Findings (54 total)

The original H-SDK10 list flagged 20 exports for deprecation; verification
during Phase 9 OBSERVE revised the count to 54 hidden barrel exports per
`sdk/kit/CHANGELOG.md` line 50 (Sprint 1 barrel closeout entry).

### Removed from this list (per Council D-1 / verification)

- `OwnerClient` — verified internally consumed by `vault-handle.ts:31`,
  `sigil.ts:39`, `dashboard/mutations.ts` delegation patterns. Stays
  in the public surface. ISC-30..34 add new wrapper methods on the
  same class.
- `SQUADS_V4_PROGRAM_ID`, `detectSquadsV4Owner`, `SquadsDetectionResult`
  — re-exported via the new `multisig-detection.ts` module (Phase 9
  Batch E). Now part of the documented surface alongside the stricter
  `isSquadsV4Owned`.

### Audit data sources

- `sdk/kit/CHANGELOG.md` (lines 50-64): Sprint 1 barrel closeout that
  detailed the 54-export footprint.
- Phase 9 OBSERVE verification report (subagent invocation
  2026-05-20T18:25:36 — see PRD §Decisions D-6).
- `sdk/kit/etc/kit.api.txt` (generated by `pnpm check-surface` —
  Phase 9 Batch G). Run on a fresh `pnpm build` to enumerate the
  current public surface authoritatively.

## v0.17 prep plan

When the v0.17 prep phase begins:

1. Rerun `pnpm check-surface` against the then-current SDK.
2. Grep external `sigil-dashboard/` for every export listed in the
   refreshed manifest. Capture grep transcripts in a follow-up
   `dead-export-audit-v0.17.md`.
3. Apply `@deprecated` JSDoc using the pre-tag template above. ONLY
   tag exports with zero external consumers.
4. Plan removal at v0.17. Document the migration recipes in the v0.17
   `MIGRATION.md`.

## Scope boundary

This audit DOES NOT recommend deletion of any export in Phase 9. The
plan-track ratification was explicit: "AUDIT ONLY. NO `@deprecated`
tagging in Phase 9 (defer to v0.17 prep phase)." See PRD D-6.
