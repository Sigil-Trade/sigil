/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PERPETUALS_PROGRAM_ADDRESS } from "../programs";
import {
  getInternalPriceDecoder,
  getInternalPriceEncoder,
  type InternalPrice,
  type InternalPriceArgs,
} from "../types";

export const SET_INTERNAL_CURRENT_PRICE_DISCRIMINATOR = new Uint8Array([
  187, 242, 45, 203, 214, 7, 211, 213,
]);

export function getSetInternalCurrentPriceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_INTERNAL_CURRENT_PRICE_DISCRIMINATOR,
  );
}

export type SetInternalCurrentPriceInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type SetInternalCurrentPriceInstructionData = {
  discriminator: ReadonlyUint8Array;
  useCurrentTime: number;
  prices: Array<InternalPrice>;
};

export type SetInternalCurrentPriceInstructionDataArgs = {
  useCurrentTime: number;
  prices: Array<InternalPriceArgs>;
};

export function getSetInternalCurrentPriceInstructionDataEncoder(): Encoder<SetInternalCurrentPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["useCurrentTime", getU8Encoder()],
      ["prices", getArrayEncoder(getInternalPriceEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_INTERNAL_CURRENT_PRICE_DISCRIMINATOR,
    }),
  );
}

export function getSetInternalCurrentPriceInstructionDataDecoder(): Decoder<SetInternalCurrentPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["useCurrentTime", getU8Decoder()],
    ["prices", getArrayDecoder(getInternalPriceDecoder())],
  ]);
}

export function getSetInternalCurrentPriceInstructionDataCodec(): Codec<
  SetInternalCurrentPriceInstructionDataArgs,
  SetInternalCurrentPriceInstructionData
> {
  return combineCodec(
    getSetInternalCurrentPriceInstructionDataEncoder(),
    getSetInternalCurrentPriceInstructionDataDecoder(),
  );
}

export type SetInternalCurrentPriceInput<
  TAccountAuthority extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  useCurrentTime: SetInternalCurrentPriceInstructionDataArgs["useCurrentTime"];
  prices: SetInternalCurrentPriceInstructionDataArgs["prices"];
};

export function getSetInternalCurrentPriceInstruction<
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof PERPETUALS_PROGRAM_ADDRESS,
>(
  input: SetInternalCurrentPriceInput<TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): SetInternalCurrentPriceInstruction<TProgramAddress, TAccountAuthority> {
  // Program address.
  const programAddress = config?.programAddress ?? PERPETUALS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("authority", accounts.authority)],
    data: getSetInternalCurrentPriceInstructionDataEncoder().encode(
      args as SetInternalCurrentPriceInstructionDataArgs,
    ),
    programAddress,
  } as SetInternalCurrentPriceInstruction<TProgramAddress, TAccountAuthority>);
}

export type ParsedSetInternalCurrentPriceInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
  };
  data: SetInternalCurrentPriceInstructionData;
};

export function parseSetInternalCurrentPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetInternalCurrentPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount() },
    data: getSetInternalCurrentPriceInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
