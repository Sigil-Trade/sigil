/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PERPETUALS_PROGRAM_ADDRESS } from "../programs";
import {
  getInternalEmaPriceDecoder,
  getInternalEmaPriceEncoder,
  type InternalEmaPrice,
  type InternalEmaPriceArgs,
} from "../types";

export const SET_INTERNAL_EMA_PRICE_DISCRIMINATOR = new Uint8Array([
  46, 30, 57, 7, 225, 198, 92, 164,
]);

export function getSetInternalEmaPriceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_INTERNAL_EMA_PRICE_DISCRIMINATOR,
  );
}

export type SetInternalEmaPriceInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type SetInternalEmaPriceInstructionData = {
  discriminator: ReadonlyUint8Array;
  prices: Array<InternalEmaPrice>;
};

export type SetInternalEmaPriceInstructionDataArgs = {
  prices: Array<InternalEmaPriceArgs>;
};

export function getSetInternalEmaPriceInstructionDataEncoder(): Encoder<SetInternalEmaPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["prices", getArrayEncoder(getInternalEmaPriceEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_INTERNAL_EMA_PRICE_DISCRIMINATOR,
    }),
  );
}

export function getSetInternalEmaPriceInstructionDataDecoder(): Decoder<SetInternalEmaPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["prices", getArrayDecoder(getInternalEmaPriceDecoder())],
  ]);
}

export function getSetInternalEmaPriceInstructionDataCodec(): Codec<
  SetInternalEmaPriceInstructionDataArgs,
  SetInternalEmaPriceInstructionData
> {
  return combineCodec(
    getSetInternalEmaPriceInstructionDataEncoder(),
    getSetInternalEmaPriceInstructionDataDecoder(),
  );
}

export type SetInternalEmaPriceInput<
  TAccountAuthority extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  prices: SetInternalEmaPriceInstructionDataArgs["prices"];
};

export function getSetInternalEmaPriceInstruction<
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof PERPETUALS_PROGRAM_ADDRESS,
>(
  input: SetInternalEmaPriceInput<TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): SetInternalEmaPriceInstruction<TProgramAddress, TAccountAuthority> {
  // Program address.
  const programAddress = config?.programAddress ?? PERPETUALS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("authority", accounts.authority)],
    data: getSetInternalEmaPriceInstructionDataEncoder().encode(
      args as SetInternalEmaPriceInstructionDataArgs,
    ),
    programAddress,
  } as SetInternalEmaPriceInstruction<TProgramAddress, TAccountAuthority>);
}

export type ParsedSetInternalEmaPriceInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
  };
  data: SetInternalEmaPriceInstructionData;
};

export function parseSetInternalEmaPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetInternalEmaPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount() },
    data: getSetInternalEmaPriceInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
