/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PERPETUALS_PROGRAM_ADDRESS } from "../programs";
import {
  getPermissionsDecoder,
  getPermissionsEncoder,
  type Permissions,
  type PermissionsArgs,
} from "../types";

export const SET_POOL_CONFIG_DISCRIMINATOR = new Uint8Array([
  216, 87, 65, 125, 113, 110, 185, 120,
]);

export function getSetPoolConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_POOL_CONFIG_DISCRIMINATOR,
  );
}

export type SetPoolConfigInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountMultisig extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMultisig extends string
        ? WritableAccount<TAccountMultisig>
        : TAccountMultisig,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetPoolConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  permissions: Permissions;
  oracleAuthority: Address;
  maxAumUsd: bigint;
  stakingFeeShareBps: bigint;
  vpVolumeFactor: number;
  stakingFeeBoostBps: Array<bigint>;
  minLpPriceUsd: bigint;
  maxLpPriceUsd: bigint;
  thresholdUsd: bigint;
};

export type SetPoolConfigInstructionDataArgs = {
  permissions: PermissionsArgs;
  oracleAuthority: Address;
  maxAumUsd: number | bigint;
  stakingFeeShareBps: number | bigint;
  vpVolumeFactor: number;
  stakingFeeBoostBps: Array<number | bigint>;
  minLpPriceUsd: number | bigint;
  maxLpPriceUsd: number | bigint;
  thresholdUsd: number | bigint;
};

export function getSetPoolConfigInstructionDataEncoder(): FixedSizeEncoder<SetPoolConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["permissions", getPermissionsEncoder()],
      ["oracleAuthority", getAddressEncoder()],
      ["maxAumUsd", getU64Encoder()],
      ["stakingFeeShareBps", getU64Encoder()],
      ["vpVolumeFactor", getU8Encoder()],
      ["stakingFeeBoostBps", getArrayEncoder(getU64Encoder(), { size: 6 })],
      ["minLpPriceUsd", getU64Encoder()],
      ["maxLpPriceUsd", getU64Encoder()],
      ["thresholdUsd", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_POOL_CONFIG_DISCRIMINATOR }),
  );
}

export function getSetPoolConfigInstructionDataDecoder(): FixedSizeDecoder<SetPoolConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["permissions", getPermissionsDecoder()],
    ["oracleAuthority", getAddressDecoder()],
    ["maxAumUsd", getU64Decoder()],
    ["stakingFeeShareBps", getU64Decoder()],
    ["vpVolumeFactor", getU8Decoder()],
    ["stakingFeeBoostBps", getArrayDecoder(getU64Decoder(), { size: 6 })],
    ["minLpPriceUsd", getU64Decoder()],
    ["maxLpPriceUsd", getU64Decoder()],
    ["thresholdUsd", getU64Decoder()],
  ]);
}

export function getSetPoolConfigInstructionDataCodec(): FixedSizeCodec<
  SetPoolConfigInstructionDataArgs,
  SetPoolConfigInstructionData
> {
  return combineCodec(
    getSetPoolConfigInstructionDataEncoder(),
    getSetPoolConfigInstructionDataDecoder(),
  );
}

export type SetPoolConfigAsyncInput<
  TAccountAdmin extends string = string,
  TAccountMultisig extends string = string,
  TAccountPool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  multisig?: Address<TAccountMultisig>;
  pool: Address<TAccountPool>;
  systemProgram?: Address<TAccountSystemProgram>;
  permissions: SetPoolConfigInstructionDataArgs["permissions"];
  oracleAuthority: SetPoolConfigInstructionDataArgs["oracleAuthority"];
  maxAumUsd: SetPoolConfigInstructionDataArgs["maxAumUsd"];
  stakingFeeShareBps: SetPoolConfigInstructionDataArgs["stakingFeeShareBps"];
  vpVolumeFactor: SetPoolConfigInstructionDataArgs["vpVolumeFactor"];
  stakingFeeBoostBps: SetPoolConfigInstructionDataArgs["stakingFeeBoostBps"];
  minLpPriceUsd: SetPoolConfigInstructionDataArgs["minLpPriceUsd"];
  maxLpPriceUsd: SetPoolConfigInstructionDataArgs["maxLpPriceUsd"];
  thresholdUsd: SetPoolConfigInstructionDataArgs["thresholdUsd"];
};

export async function getSetPoolConfigInstructionAsync<
  TAccountAdmin extends string,
  TAccountMultisig extends string,
  TAccountPool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERPETUALS_PROGRAM_ADDRESS,
>(
  input: SetPoolConfigAsyncInput<
    TAccountAdmin,
    TAccountMultisig,
    TAccountPool,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetPoolConfigInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMultisig,
    TAccountPool,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERPETUALS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    multisig: { value: input.multisig ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.multisig.value) {
    accounts.multisig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([109, 117, 108, 116, 105, 115, 105, 103]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("multisig", accounts.multisig),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSetPoolConfigInstructionDataEncoder().encode(
      args as SetPoolConfigInstructionDataArgs,
    ),
    programAddress,
  } as SetPoolConfigInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMultisig,
    TAccountPool,
    TAccountSystemProgram
  >);
}

export type SetPoolConfigInput<
  TAccountAdmin extends string = string,
  TAccountMultisig extends string = string,
  TAccountPool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  multisig: Address<TAccountMultisig>;
  pool: Address<TAccountPool>;
  systemProgram?: Address<TAccountSystemProgram>;
  permissions: SetPoolConfigInstructionDataArgs["permissions"];
  oracleAuthority: SetPoolConfigInstructionDataArgs["oracleAuthority"];
  maxAumUsd: SetPoolConfigInstructionDataArgs["maxAumUsd"];
  stakingFeeShareBps: SetPoolConfigInstructionDataArgs["stakingFeeShareBps"];
  vpVolumeFactor: SetPoolConfigInstructionDataArgs["vpVolumeFactor"];
  stakingFeeBoostBps: SetPoolConfigInstructionDataArgs["stakingFeeBoostBps"];
  minLpPriceUsd: SetPoolConfigInstructionDataArgs["minLpPriceUsd"];
  maxLpPriceUsd: SetPoolConfigInstructionDataArgs["maxLpPriceUsd"];
  thresholdUsd: SetPoolConfigInstructionDataArgs["thresholdUsd"];
};

export function getSetPoolConfigInstruction<
  TAccountAdmin extends string,
  TAccountMultisig extends string,
  TAccountPool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERPETUALS_PROGRAM_ADDRESS,
>(
  input: SetPoolConfigInput<
    TAccountAdmin,
    TAccountMultisig,
    TAccountPool,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SetPoolConfigInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountMultisig,
  TAccountPool,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERPETUALS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    multisig: { value: input.multisig ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("multisig", accounts.multisig),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSetPoolConfigInstructionDataEncoder().encode(
      args as SetPoolConfigInstructionDataArgs,
    ),
    programAddress,
  } as SetPoolConfigInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMultisig,
    TAccountPool,
    TAccountSystemProgram
  >);
}

export type ParsedSetPoolConfigInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    multisig: TAccountMetas[1];
    pool: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SetPoolConfigInstructionData;
};

export function parseSetPoolConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetPoolConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      multisig: getNextAccount(),
      pool: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetPoolConfigInstructionDataDecoder().decode(instruction.data),
  };
}
