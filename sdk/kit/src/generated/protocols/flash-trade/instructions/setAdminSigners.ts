/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PERPETUALS_PROGRAM_ADDRESS } from "../programs";

export const SET_ADMIN_SIGNERS_DISCRIMINATOR = new Uint8Array([
  240, 171, 141, 105, 124, 2, 225, 188,
]);

export function getSetAdminSignersDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_ADMIN_SIGNERS_DISCRIMINATOR,
  );
}

export type SetAdminSignersInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountMultisig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMultisig extends string
        ? WritableAccount<TAccountMultisig>
        : TAccountMultisig,
      ...TRemainingAccounts,
    ]
  >;

export type SetAdminSignersInstructionData = {
  discriminator: ReadonlyUint8Array;
  minSignatures: number;
};

export type SetAdminSignersInstructionDataArgs = { minSignatures: number };

export function getSetAdminSignersInstructionDataEncoder(): FixedSizeEncoder<SetAdminSignersInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["minSignatures", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_ADMIN_SIGNERS_DISCRIMINATOR }),
  );
}

export function getSetAdminSignersInstructionDataDecoder(): FixedSizeDecoder<SetAdminSignersInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["minSignatures", getU8Decoder()],
  ]);
}

export function getSetAdminSignersInstructionDataCodec(): FixedSizeCodec<
  SetAdminSignersInstructionDataArgs,
  SetAdminSignersInstructionData
> {
  return combineCodec(
    getSetAdminSignersInstructionDataEncoder(),
    getSetAdminSignersInstructionDataDecoder(),
  );
}

export type SetAdminSignersAsyncInput<
  TAccountAdmin extends string = string,
  TAccountMultisig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  multisig?: Address<TAccountMultisig>;
  minSignatures: SetAdminSignersInstructionDataArgs["minSignatures"];
};

export async function getSetAdminSignersInstructionAsync<
  TAccountAdmin extends string,
  TAccountMultisig extends string,
  TProgramAddress extends Address = typeof PERPETUALS_PROGRAM_ADDRESS,
>(
  input: SetAdminSignersAsyncInput<TAccountAdmin, TAccountMultisig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetAdminSignersInstruction<TProgramAddress, TAccountAdmin, TAccountMultisig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERPETUALS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    multisig: { value: input.multisig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.multisig.value) {
    accounts.multisig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([109, 117, 108, 116, 105, 115, 105, 103]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("multisig", accounts.multisig),
    ],
    data: getSetAdminSignersInstructionDataEncoder().encode(
      args as SetAdminSignersInstructionDataArgs,
    ),
    programAddress,
  } as SetAdminSignersInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMultisig
  >);
}

export type SetAdminSignersInput<
  TAccountAdmin extends string = string,
  TAccountMultisig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  multisig: Address<TAccountMultisig>;
  minSignatures: SetAdminSignersInstructionDataArgs["minSignatures"];
};

export function getSetAdminSignersInstruction<
  TAccountAdmin extends string,
  TAccountMultisig extends string,
  TProgramAddress extends Address = typeof PERPETUALS_PROGRAM_ADDRESS,
>(
  input: SetAdminSignersInput<TAccountAdmin, TAccountMultisig>,
  config?: { programAddress?: TProgramAddress },
): SetAdminSignersInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountMultisig
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERPETUALS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    multisig: { value: input.multisig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("multisig", accounts.multisig),
    ],
    data: getSetAdminSignersInstructionDataEncoder().encode(
      args as SetAdminSignersInstructionDataArgs,
    ),
    programAddress,
  } as SetAdminSignersInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMultisig
  >);
}

export type ParsedSetAdminSignersInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    multisig: TAccountMetas[1];
  };
  data: SetAdminSignersInstructionData;
};

export function parseSetAdminSignersInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetAdminSignersInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), multisig: getNextAccount() },
    data: getSetAdminSignersInstructionDataDecoder().decode(instruction.data),
  };
}
