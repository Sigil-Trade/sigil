/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const APPLY_PENDING_POLICY_DISCRIMINATOR = new Uint8Array([
  114, 212, 19, 227, 89, 199, 74, 62,
]);

export function getApplyPendingPolicyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPLY_PENDING_POLICY_DISCRIMINATOR,
  );
}

export type ApplyPendingPolicyInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPolicy extends string | AccountMeta<string> = string,
  TAccountPendingPolicy extends string | AccountMeta<string> = string,
  TAccountAuditLogSuccess extends string | AccountMeta<string> = string,
  TAccountSlotHashesSysvar extends string | AccountMeta<string> =
    "SysvarS1otHashes111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountPolicy extends string
        ? WritableAccount<TAccountPolicy>
        : TAccountPolicy,
      TAccountPendingPolicy extends string
        ? WritableAccount<TAccountPendingPolicy>
        : TAccountPendingPolicy,
      TAccountAuditLogSuccess extends string
        ? WritableAccount<TAccountAuditLogSuccess>
        : TAccountAuditLogSuccess,
      TAccountSlotHashesSysvar extends string
        ? ReadonlyAccount<TAccountSlotHashesSysvar>
        : TAccountSlotHashesSysvar,
      ...TRemainingAccounts,
    ]
  >;

export type ApplyPendingPolicyInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ApplyPendingPolicyInstructionDataArgs = {};

export function getApplyPendingPolicyInstructionDataEncoder(): FixedSizeEncoder<ApplyPendingPolicyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: APPLY_PENDING_POLICY_DISCRIMINATOR,
    }),
  );
}

export function getApplyPendingPolicyInstructionDataDecoder(): FixedSizeDecoder<ApplyPendingPolicyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApplyPendingPolicyInstructionDataCodec(): FixedSizeCodec<
  ApplyPendingPolicyInstructionDataArgs,
  ApplyPendingPolicyInstructionData
> {
  return combineCodec(
    getApplyPendingPolicyInstructionDataEncoder(),
    getApplyPendingPolicyInstructionDataDecoder(),
  );
}

export type ApplyPendingPolicyAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountPendingPolicy extends string = string,
  TAccountAuditLogSuccess extends string = string,
  TAccountSlotHashesSysvar extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy?: Address<TAccountPolicy>;
  pendingPolicy?: Address<TAccountPendingPolicy>;
  /** Phase 7 — success audit log; entry appended after policy applied. */
  auditLogSuccess?: Address<TAccountAuditLogSuccess>;
  slotHashesSysvar?: Address<TAccountSlotHashesSysvar>;
};

export async function getApplyPendingPolicyInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountPendingPolicy extends string,
  TAccountAuditLogSuccess extends string,
  TAccountSlotHashesSysvar extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: ApplyPendingPolicyAsyncInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ApplyPendingPolicyInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: true },
    pendingPolicy: { value: input.pendingPolicy ?? null, isWritable: true },
    auditLogSuccess: { value: input.auditLogSuccess ?? null, isWritable: true },
    slotHashesSysvar: {
      value: input.slotHashesSysvar ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.policy.value) {
    accounts.policy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 111, 108, 105, 99, 121])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.pendingPolicy.value) {
    accounts.pendingPolicy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 101, 110, 100, 105, 110, 103, 95, 112, 111, 108, 105, 99, 121,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.auditLogSuccess.value) {
    accounts.auditLogSuccess.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            97, 117, 100, 105, 116, 95, 115, 117, 99, 99, 101, 115, 115,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.slotHashesSysvar.value) {
    accounts.slotHashesSysvar.value =
      "SysvarS1otHashes111111111111111111111111111" as Address<"SysvarS1otHashes111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("pendingPolicy", accounts.pendingPolicy),
      getAccountMeta("auditLogSuccess", accounts.auditLogSuccess),
      getAccountMeta("slotHashesSysvar", accounts.slotHashesSysvar),
    ],
    data: getApplyPendingPolicyInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyPendingPolicyInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar
  >);
}

export type ApplyPendingPolicyInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountPendingPolicy extends string = string,
  TAccountAuditLogSuccess extends string = string,
  TAccountSlotHashesSysvar extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy: Address<TAccountPolicy>;
  pendingPolicy: Address<TAccountPendingPolicy>;
  /** Phase 7 — success audit log; entry appended after policy applied. */
  auditLogSuccess: Address<TAccountAuditLogSuccess>;
  slotHashesSysvar?: Address<TAccountSlotHashesSysvar>;
};

export function getApplyPendingPolicyInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountPendingPolicy extends string,
  TAccountAuditLogSuccess extends string,
  TAccountSlotHashesSysvar extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: ApplyPendingPolicyInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar
  >,
  config?: { programAddress?: TProgramAddress },
): ApplyPendingPolicyInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountPolicy,
  TAccountPendingPolicy,
  TAccountAuditLogSuccess,
  TAccountSlotHashesSysvar
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: true },
    pendingPolicy: { value: input.pendingPolicy ?? null, isWritable: true },
    auditLogSuccess: { value: input.auditLogSuccess ?? null, isWritable: true },
    slotHashesSysvar: {
      value: input.slotHashesSysvar ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.slotHashesSysvar.value) {
    accounts.slotHashesSysvar.value =
      "SysvarS1otHashes111111111111111111111111111" as Address<"SysvarS1otHashes111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("pendingPolicy", accounts.pendingPolicy),
      getAccountMeta("auditLogSuccess", accounts.auditLogSuccess),
      getAccountMeta("slotHashesSysvar", accounts.slotHashesSysvar),
    ],
    data: getApplyPendingPolicyInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyPendingPolicyInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar
  >);
}

export type ParsedApplyPendingPolicyInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    policy: TAccountMetas[2];
    pendingPolicy: TAccountMetas[3];
    /** Phase 7 — success audit log; entry appended after policy applied. */
    auditLogSuccess: TAccountMetas[4];
    slotHashesSysvar: TAccountMetas[5];
  };
  data: ApplyPendingPolicyInstructionData;
};

export function parseApplyPendingPolicyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApplyPendingPolicyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      policy: getNextAccount(),
      pendingPolicy: getNextAccount(),
      auditLogSuccess: getNextAccount(),
      slotHashesSysvar: getNextAccount(),
    },
    data: getApplyPendingPolicyInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
