/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const CLOSE_SETTLED_ESCROW_DISCRIMINATOR = new Uint8Array([
  169, 244, 164, 173, 181, 214, 139, 6,
]);

export function getCloseSettledEscrowDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_SETTLED_ESCROW_DISCRIMINATOR,
  );
}

export type CloseSettledEscrowInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSourceVault extends string | AccountMeta<string> = string,
  TAccountDestinationVaultKey extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSourceVault extends string
        ? ReadonlyAccount<TAccountSourceVault>
        : TAccountSourceVault,
      TAccountDestinationVaultKey extends string
        ? ReadonlyAccount<TAccountDestinationVaultKey>
        : TAccountDestinationVaultKey,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      ...TRemainingAccounts,
    ]
  >;

export type CloseSettledEscrowInstructionData = {
  discriminator: ReadonlyUint8Array;
  escrowId: bigint;
};

export type CloseSettledEscrowInstructionDataArgs = {
  escrowId: number | bigint;
};

export function getCloseSettledEscrowInstructionDataEncoder(): FixedSizeEncoder<CloseSettledEscrowInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["escrowId", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CLOSE_SETTLED_ESCROW_DISCRIMINATOR,
    }),
  );
}

export function getCloseSettledEscrowInstructionDataDecoder(): FixedSizeDecoder<CloseSettledEscrowInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["escrowId", getU64Decoder()],
  ]);
}

export function getCloseSettledEscrowInstructionDataCodec(): FixedSizeCodec<
  CloseSettledEscrowInstructionDataArgs,
  CloseSettledEscrowInstructionData
> {
  return combineCodec(
    getCloseSettledEscrowInstructionDataEncoder(),
    getCloseSettledEscrowInstructionDataDecoder(),
  );
}

export type CloseSettledEscrowAsyncInput<
  TAccountSigner extends string = string,
  TAccountSourceVault extends string = string,
  TAccountDestinationVaultKey extends string = string,
  TAccountEscrow extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  sourceVault: Address<TAccountSourceVault>;
  /**
   * Validated indirectly: if the wrong key is passed, the escrow PDA seeds won't
   * match and Anchor will reject the account.
   */
  destinationVaultKey: Address<TAccountDestinationVaultKey>;
  escrow?: Address<TAccountEscrow>;
  escrowId: CloseSettledEscrowInstructionDataArgs["escrowId"];
};

export async function getCloseSettledEscrowInstructionAsync<
  TAccountSigner extends string,
  TAccountSourceVault extends string,
  TAccountDestinationVaultKey extends string,
  TAccountEscrow extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: CloseSettledEscrowAsyncInput<
    TAccountSigner,
    TAccountSourceVault,
    TAccountDestinationVaultKey,
    TAccountEscrow
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseSettledEscrowInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSourceVault,
    TAccountDestinationVaultKey,
    TAccountEscrow
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    sourceVault: { value: input.sourceVault ?? null, isWritable: false },
    destinationVaultKey: {
      value: input.destinationVaultKey ?? null,
      isWritable: false,
    },
    escrow: { value: input.escrow ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.escrow.value) {
    accounts.escrow.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([101, 115, 99, 114, 111, 119])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "sourceVault",
            accounts.sourceVault.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "destinationVaultKey",
            accounts.destinationVaultKey.value,
          ),
        ),
        getU64Encoder().encode(
          getNonNullResolvedInstructionInput("escrowId", args.escrowId),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("sourceVault", accounts.sourceVault),
      getAccountMeta("destinationVaultKey", accounts.destinationVaultKey),
      getAccountMeta("escrow", accounts.escrow),
    ],
    data: getCloseSettledEscrowInstructionDataEncoder().encode(
      args as CloseSettledEscrowInstructionDataArgs,
    ),
    programAddress,
  } as CloseSettledEscrowInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSourceVault,
    TAccountDestinationVaultKey,
    TAccountEscrow
  >);
}

export type CloseSettledEscrowInput<
  TAccountSigner extends string = string,
  TAccountSourceVault extends string = string,
  TAccountDestinationVaultKey extends string = string,
  TAccountEscrow extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  sourceVault: Address<TAccountSourceVault>;
  /**
   * Validated indirectly: if the wrong key is passed, the escrow PDA seeds won't
   * match and Anchor will reject the account.
   */
  destinationVaultKey: Address<TAccountDestinationVaultKey>;
  escrow: Address<TAccountEscrow>;
  escrowId: CloseSettledEscrowInstructionDataArgs["escrowId"];
};

export function getCloseSettledEscrowInstruction<
  TAccountSigner extends string,
  TAccountSourceVault extends string,
  TAccountDestinationVaultKey extends string,
  TAccountEscrow extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: CloseSettledEscrowInput<
    TAccountSigner,
    TAccountSourceVault,
    TAccountDestinationVaultKey,
    TAccountEscrow
  >,
  config?: { programAddress?: TProgramAddress },
): CloseSettledEscrowInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountSourceVault,
  TAccountDestinationVaultKey,
  TAccountEscrow
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    sourceVault: { value: input.sourceVault ?? null, isWritable: false },
    destinationVaultKey: {
      value: input.destinationVaultKey ?? null,
      isWritable: false,
    },
    escrow: { value: input.escrow ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("sourceVault", accounts.sourceVault),
      getAccountMeta("destinationVaultKey", accounts.destinationVaultKey),
      getAccountMeta("escrow", accounts.escrow),
    ],
    data: getCloseSettledEscrowInstructionDataEncoder().encode(
      args as CloseSettledEscrowInstructionDataArgs,
    ),
    programAddress,
  } as CloseSettledEscrowInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSourceVault,
    TAccountDestinationVaultKey,
    TAccountEscrow
  >);
}

export type ParsedCloseSettledEscrowInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    sourceVault: TAccountMetas[1];
    /**
     * Validated indirectly: if the wrong key is passed, the escrow PDA seeds won't
     * match and Anchor will reject the account.
     */
    destinationVaultKey: TAccountMetas[2];
    escrow: TAccountMetas[3];
  };
  data: CloseSettledEscrowInstructionData;
};

export function parseCloseSettledEscrowInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseSettledEscrowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      sourceVault: getNextAccount(),
      destinationVaultKey: getNextAccount(),
      escrow: getNextAccount(),
    },
    data: getCloseSettledEscrowInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
