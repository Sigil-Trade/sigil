/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const ACCEPT_OWNERSHIP_TRANSFER_MULTISIG_DISCRIMINATOR = new Uint8Array([
  112, 147, 61, 110, 221, 182, 203, 99,
]);

export function getAcceptOwnershipTransferMultisigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACCEPT_OWNERSHIP_TRANSFER_MULTISIG_DISCRIMINATOR,
  );
}

export type AcceptOwnershipTransferMultisigInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMultisigPda extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPolicy extends string | AccountMeta<string> = string,
  TAccountPending extends string | AccountMeta<string> = string,
  TAccountAuditLogSuccess extends string | AccountMeta<string> = string,
  TAccountSlotHashesSysvar extends string | AccountMeta<string> =
    "SysvarS1otHashes111111111111111111111111111",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMultisigPda extends string
        ? WritableAccount<TAccountMultisigPda>
        : TAccountMultisigPda,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountPolicy extends string
        ? WritableAccount<TAccountPolicy>
        : TAccountPolicy,
      TAccountPending extends string
        ? WritableAccount<TAccountPending>
        : TAccountPending,
      TAccountAuditLogSuccess extends string
        ? WritableAccount<TAccountAuditLogSuccess>
        : TAccountAuditLogSuccess,
      TAccountSlotHashesSysvar extends string
        ? ReadonlyAccount<TAccountSlotHashesSysvar>
        : TAccountSlotHashesSysvar,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptOwnershipTransferMultisigInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AcceptOwnershipTransferMultisigInstructionDataArgs = {};

export function getAcceptOwnershipTransferMultisigInstructionDataEncoder(): FixedSizeEncoder<AcceptOwnershipTransferMultisigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ACCEPT_OWNERSHIP_TRANSFER_MULTISIG_DISCRIMINATOR,
    }),
  );
}

export function getAcceptOwnershipTransferMultisigInstructionDataDecoder(): FixedSizeDecoder<AcceptOwnershipTransferMultisigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAcceptOwnershipTransferMultisigInstructionDataCodec(): FixedSizeCodec<
  AcceptOwnershipTransferMultisigInstructionDataArgs,
  AcceptOwnershipTransferMultisigInstructionData
> {
  return combineCodec(
    getAcceptOwnershipTransferMultisigInstructionDataEncoder(),
    getAcceptOwnershipTransferMultisigInstructionDataDecoder(),
  );
}

export type AcceptOwnershipTransferMultisigAsyncInput<
  TAccountMultisigPda extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountPending extends string = string,
  TAccountAuditLogSuccess extends string = string,
  TAccountSlotHashesSysvar extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * vault PDAs have no private key — authority is enforced by:
   * 1. `owner == SQUADS_V4_PROGRAM_ID` (verified in handler)
   * 2. `key() == pending.new_owner` (verified in handler)
   * 3. `pending.is_multisig_target == true` (verified in handler)
   *
   * Marked `mut` so the closed `pending` PDA's rent returns here.
   */
  multisigPda: Address<TAccountMultisigPda>;
  /**
   * Vault is mutated (owner field overwritten). PDA derivation uses the
   * immutable `vault.vault_authority` field (LBL-01) so the seed binding
   * survives ownership transfer. Handler-level `require_keys_eq!(
   * pending.current_owner, vault.owner)` replaces the implicit seed
   * binding that previously enforced the queue→accept owner match.
   */
  vault: Address<TAccountVault>;
  /**
   * Policy is mutated (policy_version bump + `policy_preview_digest`
   * recompute — see handler lines 180-230, mirroring the EOA path).
   */
  policy?: Address<TAccountPolicy>;
  /**
   * PendingOwnershipTransfer PDA. `close = multisig_pda` returns rent to the
   * new authority. `has_one = vault` binds the PDA to this vault explicitly
   * (defense-in-depth against future seeds drift; same pattern as §RP-1 I-2).
   */
  pending?: Address<TAccountPending>;
  /** Phase 7 — success audit log; entry appended after state mutation. */
  auditLogSuccess?: Address<TAccountAuditLogSuccess>;
  slotHashesSysvar?: Address<TAccountSlotHashesSysvar>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAcceptOwnershipTransferMultisigInstructionAsync<
  TAccountMultisigPda extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountPending extends string,
  TAccountAuditLogSuccess extends string,
  TAccountSlotHashesSysvar extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: AcceptOwnershipTransferMultisigAsyncInput<
    TAccountMultisigPda,
    TAccountVault,
    TAccountPolicy,
    TAccountPending,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AcceptOwnershipTransferMultisigInstruction<
    TProgramAddress,
    TAccountMultisigPda,
    TAccountVault,
    TAccountPolicy,
    TAccountPending,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    multisigPda: { value: input.multisigPda ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    policy: { value: input.policy ?? null, isWritable: true },
    pending: { value: input.pending ?? null, isWritable: true },
    auditLogSuccess: { value: input.auditLogSuccess ?? null, isWritable: true },
    slotHashesSysvar: {
      value: input.slotHashesSysvar ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.policy.value) {
    accounts.policy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 111, 108, 105, 99, 121])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.pending.value) {
    accounts.pending.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 101, 110, 100, 105, 110, 103, 95, 111, 119, 110, 101, 114,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.auditLogSuccess.value) {
    accounts.auditLogSuccess.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            97, 117, 100, 105, 116, 95, 115, 117, 99, 99, 101, 115, 115,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.slotHashesSysvar.value) {
    accounts.slotHashesSysvar.value =
      "SysvarS1otHashes111111111111111111111111111" as Address<"SysvarS1otHashes111111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("multisigPda", accounts.multisigPda),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("pending", accounts.pending),
      getAccountMeta("auditLogSuccess", accounts.auditLogSuccess),
      getAccountMeta("slotHashesSysvar", accounts.slotHashesSysvar),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAcceptOwnershipTransferMultisigInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptOwnershipTransferMultisigInstruction<
    TProgramAddress,
    TAccountMultisigPda,
    TAccountVault,
    TAccountPolicy,
    TAccountPending,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar,
    TAccountSystemProgram
  >);
}

export type AcceptOwnershipTransferMultisigInput<
  TAccountMultisigPda extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountPending extends string = string,
  TAccountAuditLogSuccess extends string = string,
  TAccountSlotHashesSysvar extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * vault PDAs have no private key — authority is enforced by:
   * 1. `owner == SQUADS_V4_PROGRAM_ID` (verified in handler)
   * 2. `key() == pending.new_owner` (verified in handler)
   * 3. `pending.is_multisig_target == true` (verified in handler)
   *
   * Marked `mut` so the closed `pending` PDA's rent returns here.
   */
  multisigPda: Address<TAccountMultisigPda>;
  /**
   * Vault is mutated (owner field overwritten). PDA derivation uses the
   * immutable `vault.vault_authority` field (LBL-01) so the seed binding
   * survives ownership transfer. Handler-level `require_keys_eq!(
   * pending.current_owner, vault.owner)` replaces the implicit seed
   * binding that previously enforced the queue→accept owner match.
   */
  vault: Address<TAccountVault>;
  /**
   * Policy is mutated (policy_version bump + `policy_preview_digest`
   * recompute — see handler lines 180-230, mirroring the EOA path).
   */
  policy: Address<TAccountPolicy>;
  /**
   * PendingOwnershipTransfer PDA. `close = multisig_pda` returns rent to the
   * new authority. `has_one = vault` binds the PDA to this vault explicitly
   * (defense-in-depth against future seeds drift; same pattern as §RP-1 I-2).
   */
  pending: Address<TAccountPending>;
  /** Phase 7 — success audit log; entry appended after state mutation. */
  auditLogSuccess: Address<TAccountAuditLogSuccess>;
  slotHashesSysvar?: Address<TAccountSlotHashesSysvar>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAcceptOwnershipTransferMultisigInstruction<
  TAccountMultisigPda extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountPending extends string,
  TAccountAuditLogSuccess extends string,
  TAccountSlotHashesSysvar extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: AcceptOwnershipTransferMultisigInput<
    TAccountMultisigPda,
    TAccountVault,
    TAccountPolicy,
    TAccountPending,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AcceptOwnershipTransferMultisigInstruction<
  TProgramAddress,
  TAccountMultisigPda,
  TAccountVault,
  TAccountPolicy,
  TAccountPending,
  TAccountAuditLogSuccess,
  TAccountSlotHashesSysvar,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    multisigPda: { value: input.multisigPda ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    policy: { value: input.policy ?? null, isWritable: true },
    pending: { value: input.pending ?? null, isWritable: true },
    auditLogSuccess: { value: input.auditLogSuccess ?? null, isWritable: true },
    slotHashesSysvar: {
      value: input.slotHashesSysvar ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.slotHashesSysvar.value) {
    accounts.slotHashesSysvar.value =
      "SysvarS1otHashes111111111111111111111111111" as Address<"SysvarS1otHashes111111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("multisigPda", accounts.multisigPda),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("pending", accounts.pending),
      getAccountMeta("auditLogSuccess", accounts.auditLogSuccess),
      getAccountMeta("slotHashesSysvar", accounts.slotHashesSysvar),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAcceptOwnershipTransferMultisigInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptOwnershipTransferMultisigInstruction<
    TProgramAddress,
    TAccountMultisigPda,
    TAccountVault,
    TAccountPolicy,
    TAccountPending,
    TAccountAuditLogSuccess,
    TAccountSlotHashesSysvar,
    TAccountSystemProgram
  >);
}

export type ParsedAcceptOwnershipTransferMultisigInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * vault PDAs have no private key — authority is enforced by:
     * 1. `owner == SQUADS_V4_PROGRAM_ID` (verified in handler)
     * 2. `key() == pending.new_owner` (verified in handler)
     * 3. `pending.is_multisig_target == true` (verified in handler)
     *
     * Marked `mut` so the closed `pending` PDA's rent returns here.
     */
    multisigPda: TAccountMetas[0];
    /**
     * Vault is mutated (owner field overwritten). PDA derivation uses the
     * immutable `vault.vault_authority` field (LBL-01) so the seed binding
     * survives ownership transfer. Handler-level `require_keys_eq!(
     * pending.current_owner, vault.owner)` replaces the implicit seed
     * binding that previously enforced the queue→accept owner match.
     */
    vault: TAccountMetas[1];
    /**
     * Policy is mutated (policy_version bump + `policy_preview_digest`
     * recompute — see handler lines 180-230, mirroring the EOA path).
     */
    policy: TAccountMetas[2];
    /**
     * PendingOwnershipTransfer PDA. `close = multisig_pda` returns rent to the
     * new authority. `has_one = vault` binds the PDA to this vault explicitly
     * (defense-in-depth against future seeds drift; same pattern as §RP-1 I-2).
     */
    pending: TAccountMetas[3];
    /** Phase 7 — success audit log; entry appended after state mutation. */
    auditLogSuccess: TAccountMetas[4];
    slotHashesSysvar: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: AcceptOwnershipTransferMultisigInstructionData;
};

export function parseAcceptOwnershipTransferMultisigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAcceptOwnershipTransferMultisigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      multisigPda: getNextAccount(),
      vault: getNextAccount(),
      policy: getNextAccount(),
      pending: getNextAccount(),
      auditLogSuccess: getNextAccount(),
      slotHashesSysvar: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAcceptOwnershipTransferMultisigInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
