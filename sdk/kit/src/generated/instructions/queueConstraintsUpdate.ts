/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  getConstraintEntryDecoder,
  getConstraintEntryEncoder,
  type ConstraintEntry,
  type ConstraintEntryArgs,
} from "../types/index.js";

export const QUEUE_CONSTRAINTS_UPDATE_DISCRIMINATOR = new Uint8Array([
  247, 253, 233, 93, 233, 54, 53, 131,
]);

export function getQueueConstraintsUpdateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    QUEUE_CONSTRAINTS_UPDATE_DISCRIMINATOR,
  );
}

export type QueueConstraintsUpdateInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPolicy extends string | AccountMeta<string> = string,
  TAccountConstraints extends string | AccountMeta<string> = string,
  TAccountPendingConstraints extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountPolicy extends string
        ? ReadonlyAccount<TAccountPolicy>
        : TAccountPolicy,
      TAccountConstraints extends string
        ? ReadonlyAccount<TAccountConstraints>
        : TAccountConstraints,
      TAccountPendingConstraints extends string
        ? WritableAccount<TAccountPendingConstraints>
        : TAccountPendingConstraints,
      ...TRemainingAccounts,
    ]
  >;

export type QueueConstraintsUpdateInstructionData = {
  discriminator: ReadonlyUint8Array;
  entries: Array<ConstraintEntry>;
  strictMode: boolean;
};

export type QueueConstraintsUpdateInstructionDataArgs = {
  entries: Array<ConstraintEntryArgs>;
  strictMode: boolean;
};

export function getQueueConstraintsUpdateInstructionDataEncoder(): Encoder<QueueConstraintsUpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["entries", getArrayEncoder(getConstraintEntryEncoder())],
      ["strictMode", getBooleanEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: QUEUE_CONSTRAINTS_UPDATE_DISCRIMINATOR,
    }),
  );
}

export function getQueueConstraintsUpdateInstructionDataDecoder(): Decoder<QueueConstraintsUpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["entries", getArrayDecoder(getConstraintEntryDecoder())],
    ["strictMode", getBooleanDecoder()],
  ]);
}

export function getQueueConstraintsUpdateInstructionDataCodec(): Codec<
  QueueConstraintsUpdateInstructionDataArgs,
  QueueConstraintsUpdateInstructionData
> {
  return combineCodec(
    getQueueConstraintsUpdateInstructionDataEncoder(),
    getQueueConstraintsUpdateInstructionDataDecoder(),
  );
}

export type QueueConstraintsUpdateAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountConstraints extends string = string,
  TAccountPendingConstraints extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy?: Address<TAccountPolicy>;
  /** Existing constraints — seeds verify PDA, bump verified via load(). */
  constraints?: Address<TAccountConstraints>;
  /** Verified in handler: correct size, program-owned, vault match, no discriminator. */
  pendingConstraints?: Address<TAccountPendingConstraints>;
  entries: QueueConstraintsUpdateInstructionDataArgs["entries"];
  strictMode: QueueConstraintsUpdateInstructionDataArgs["strictMode"];
};

export async function getQueueConstraintsUpdateInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountConstraints extends string,
  TAccountPendingConstraints extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: QueueConstraintsUpdateAsyncInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingConstraints
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  QueueConstraintsUpdateInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingConstraints
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: false },
    constraints: { value: input.constraints ?? null, isWritable: false },
    pendingConstraints: {
      value: input.pendingConstraints ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.policy.value) {
    accounts.policy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 111, 108, 105, 99, 121])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.constraints.value) {
    accounts.constraints.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([99, 111, 110, 115, 116, 114, 97, 105, 110, 116, 115]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.pendingConstraints.value) {
    accounts.pendingConstraints.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 101, 110, 100, 105, 110, 103, 95, 99, 111, 110, 115, 116, 114,
            97, 105, 110, 116, 115,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("constraints", accounts.constraints),
      getAccountMeta("pendingConstraints", accounts.pendingConstraints),
    ],
    data: getQueueConstraintsUpdateInstructionDataEncoder().encode(
      args as QueueConstraintsUpdateInstructionDataArgs,
    ),
    programAddress,
  } as QueueConstraintsUpdateInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingConstraints
  >);
}

export type QueueConstraintsUpdateInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountConstraints extends string = string,
  TAccountPendingConstraints extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy: Address<TAccountPolicy>;
  /** Existing constraints — seeds verify PDA, bump verified via load(). */
  constraints: Address<TAccountConstraints>;
  /** Verified in handler: correct size, program-owned, vault match, no discriminator. */
  pendingConstraints: Address<TAccountPendingConstraints>;
  entries: QueueConstraintsUpdateInstructionDataArgs["entries"];
  strictMode: QueueConstraintsUpdateInstructionDataArgs["strictMode"];
};

export function getQueueConstraintsUpdateInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountConstraints extends string,
  TAccountPendingConstraints extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: QueueConstraintsUpdateInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingConstraints
  >,
  config?: { programAddress?: TProgramAddress },
): QueueConstraintsUpdateInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountPolicy,
  TAccountConstraints,
  TAccountPendingConstraints
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: false },
    constraints: { value: input.constraints ?? null, isWritable: false },
    pendingConstraints: {
      value: input.pendingConstraints ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("constraints", accounts.constraints),
      getAccountMeta("pendingConstraints", accounts.pendingConstraints),
    ],
    data: getQueueConstraintsUpdateInstructionDataEncoder().encode(
      args as QueueConstraintsUpdateInstructionDataArgs,
    ),
    programAddress,
  } as QueueConstraintsUpdateInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingConstraints
  >);
}

export type ParsedQueueConstraintsUpdateInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    policy: TAccountMetas[2];
    /** Existing constraints — seeds verify PDA, bump verified via load(). */
    constraints: TAccountMetas[3];
    /** Verified in handler: correct size, program-owned, vault match, no discriminator. */
    pendingConstraints: TAccountMetas[4];
  };
  data: QueueConstraintsUpdateInstructionData;
};

export function parseQueueConstraintsUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedQueueConstraintsUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      policy: getNextAccount(),
      constraints: getNextAccount(),
      pendingConstraints: getNextAccount(),
    },
    data: getQueueConstraintsUpdateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
