/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const EXTEND_PDA_DISCRIMINATOR = new Uint8Array([
  13, 211, 140, 90, 104, 28, 141, 200,
]);

export function getExtendPdaDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EXTEND_PDA_DISCRIMINATOR);
}

export type ExtendPdaInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPda extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountPda extends string ? WritableAccount<TAccountPda> : TAccountPda,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ExtendPdaInstructionData = {
  discriminator: ReadonlyUint8Array;
  targetSize: number;
};

export type ExtendPdaInstructionDataArgs = { targetSize: number };

export function getExtendPdaInstructionDataEncoder(): FixedSizeEncoder<ExtendPdaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["targetSize", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: EXTEND_PDA_DISCRIMINATOR }),
  );
}

export function getExtendPdaInstructionDataDecoder(): FixedSizeDecoder<ExtendPdaInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["targetSize", getU32Decoder()],
  ]);
}

export function getExtendPdaInstructionDataCodec(): FixedSizeCodec<
  ExtendPdaInstructionDataArgs,
  ExtendPdaInstructionData
> {
  return combineCodec(
    getExtendPdaInstructionDataEncoder(),
    getExtendPdaInstructionDataDecoder(),
  );
}

export type ExtendPdaInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPda extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  /** owner == crate::ID, vault bytes match, size within bounds. */
  pda: Address<TAccountPda>;
  systemProgram?: Address<TAccountSystemProgram>;
  targetSize: ExtendPdaInstructionDataArgs["targetSize"];
};

export function getExtendPdaInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPda extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: ExtendPdaInput<
    TAccountOwner,
    TAccountVault,
    TAccountPda,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ExtendPdaInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountPda,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    pda: { value: input.pda ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("pda", accounts.pda),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getExtendPdaInstructionDataEncoder().encode(
      args as ExtendPdaInstructionDataArgs,
    ),
    programAddress,
  } as ExtendPdaInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPda,
    TAccountSystemProgram
  >);
}

export type ParsedExtendPdaInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    /** owner == crate::ID, vault bytes match, size within bounds. */
    pda: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ExtendPdaInstructionData;
};

export function parseExtendPdaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExtendPdaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      pda: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getExtendPdaInstructionDataDecoder().decode(instruction.data),
  };
}
