/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const APPROVE_PENDING_POLICY_DISCRIMINATOR = new Uint8Array([
  151, 218, 153, 6, 155, 67, 8, 200,
]);

export function getApprovePendingPolicyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVE_PENDING_POLICY_DISCRIMINATOR,
  );
}

export type ApprovePendingPolicyInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountCosigner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPolicy extends string | AccountMeta<string> = string,
  TAccountPendingPolicy extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCosigner extends string
        ? ReadonlySignerAccount<TAccountCosigner> &
            AccountSignerMeta<TAccountCosigner>
        : TAccountCosigner,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountPolicy extends string
        ? ReadonlyAccount<TAccountPolicy>
        : TAccountPolicy,
      TAccountPendingPolicy extends string
        ? WritableAccount<TAccountPendingPolicy>
        : TAccountPendingPolicy,
      ...TRemainingAccounts,
    ]
  >;

export type ApprovePendingPolicyInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ApprovePendingPolicyInstructionDataArgs = {};

export function getApprovePendingPolicyInstructionDataEncoder(): FixedSizeEncoder<ApprovePendingPolicyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: APPROVE_PENDING_POLICY_DISCRIMINATOR,
    }),
  );
}

export function getApprovePendingPolicyInstructionDataDecoder(): FixedSizeDecoder<ApprovePendingPolicyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApprovePendingPolicyInstructionDataCodec(): FixedSizeCodec<
  ApprovePendingPolicyInstructionDataArgs,
  ApprovePendingPolicyInstructionData
> {
  return combineCodec(
    getApprovePendingPolicyInstructionDataEncoder(),
    getApprovePendingPolicyInstructionDataDecoder(),
  );
}

export type ApprovePendingPolicyAsyncInput<
  TAccountCosigner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountPendingPolicy extends string = string,
> = {
  /** The bound cosigner K. Must equal `policy.cosign_session_pubkey`. */
  cosigner: TransactionSigner<TAccountCosigner>;
  vault: Address<TAccountVault>;
  policy?: Address<TAccountPolicy>;
  pendingPolicy?: Address<TAccountPendingPolicy>;
};

export async function getApprovePendingPolicyInstructionAsync<
  TAccountCosigner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountPendingPolicy extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: ApprovePendingPolicyAsyncInput<
    TAccountCosigner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ApprovePendingPolicyInstruction<
    TProgramAddress,
    TAccountCosigner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cosigner: { value: input.cosigner ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: false },
    pendingPolicy: { value: input.pendingPolicy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.policy.value) {
    accounts.policy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 111, 108, 105, 99, 121])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.pendingPolicy.value) {
    accounts.pendingPolicy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 101, 110, 100, 105, 110, 103, 95, 112, 111, 108, 105, 99, 121,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cosigner", accounts.cosigner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("pendingPolicy", accounts.pendingPolicy),
    ],
    data: getApprovePendingPolicyInstructionDataEncoder().encode({}),
    programAddress,
  } as ApprovePendingPolicyInstruction<
    TProgramAddress,
    TAccountCosigner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy
  >);
}

export type ApprovePendingPolicyInput<
  TAccountCosigner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountPendingPolicy extends string = string,
> = {
  /** The bound cosigner K. Must equal `policy.cosign_session_pubkey`. */
  cosigner: TransactionSigner<TAccountCosigner>;
  vault: Address<TAccountVault>;
  policy: Address<TAccountPolicy>;
  pendingPolicy: Address<TAccountPendingPolicy>;
};

export function getApprovePendingPolicyInstruction<
  TAccountCosigner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountPendingPolicy extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: ApprovePendingPolicyInput<
    TAccountCosigner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy
  >,
  config?: { programAddress?: TProgramAddress },
): ApprovePendingPolicyInstruction<
  TProgramAddress,
  TAccountCosigner,
  TAccountVault,
  TAccountPolicy,
  TAccountPendingPolicy
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cosigner: { value: input.cosigner ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: false },
    pendingPolicy: { value: input.pendingPolicy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cosigner", accounts.cosigner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("pendingPolicy", accounts.pendingPolicy),
    ],
    data: getApprovePendingPolicyInstructionDataEncoder().encode({}),
    programAddress,
  } as ApprovePendingPolicyInstruction<
    TProgramAddress,
    TAccountCosigner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingPolicy
  >);
}

export type ParsedApprovePendingPolicyInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The bound cosigner K. Must equal `policy.cosign_session_pubkey`. */
    cosigner: TAccountMetas[0];
    vault: TAccountMetas[1];
    policy: TAccountMetas[2];
    pendingPolicy: TAccountMetas[3];
  };
  data: ApprovePendingPolicyInstructionData;
};

export function parseApprovePendingPolicyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApprovePendingPolicyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cosigner: getNextAccount(),
      vault: getNextAccount(),
      policy: getNextAccount(),
      pendingPolicy: getNextAccount(),
    },
    data: getApprovePendingPolicyInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
