/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const SET_OBSERVE_ONLY_DISCRIMINATOR = new Uint8Array([
  36, 88, 141, 35, 179, 134, 54, 12,
]);

export function getSetObserveOnlyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_OBSERVE_ONLY_DISCRIMINATOR,
  );
}

export type SetObserveOnlyInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPolicy extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountPolicy extends string
        ? WritableAccount<TAccountPolicy>
        : TAccountPolicy,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type SetObserveOnlyInstructionData = {
  discriminator: ReadonlyUint8Array;
  newValue: boolean;
};

export type SetObserveOnlyInstructionDataArgs = { newValue: boolean };

export function getSetObserveOnlyInstructionDataEncoder(): FixedSizeEncoder<SetObserveOnlyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newValue", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_OBSERVE_ONLY_DISCRIMINATOR }),
  );
}

export function getSetObserveOnlyInstructionDataDecoder(): FixedSizeDecoder<SetObserveOnlyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newValue", getBooleanDecoder()],
  ]);
}

export function getSetObserveOnlyInstructionDataCodec(): FixedSizeCodec<
  SetObserveOnlyInstructionDataArgs,
  SetObserveOnlyInstructionData
> {
  return combineCodec(
    getSetObserveOnlyInstructionDataEncoder(),
    getSetObserveOnlyInstructionDataDecoder(),
  );
}

export type SetObserveOnlyAsyncInput<
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountOwner extends string = string,
> = {
  vault: Address<TAccountVault>;
  policy?: Address<TAccountPolicy>;
  owner: TransactionSigner<TAccountOwner>;
  newValue: SetObserveOnlyInstructionDataArgs["newValue"];
};

export async function getSetObserveOnlyInstructionAsync<
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: SetObserveOnlyAsyncInput<TAccountVault, TAccountPolicy, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetObserveOnlyInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountPolicy,
    TAccountOwner
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    policy: { value: input.policy ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.policy.value) {
    accounts.policy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 111, 108, 105, 99, 121])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetObserveOnlyInstructionDataEncoder().encode(
      args as SetObserveOnlyInstructionDataArgs,
    ),
    programAddress,
  } as SetObserveOnlyInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountPolicy,
    TAccountOwner
  >);
}

export type SetObserveOnlyInput<
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountOwner extends string = string,
> = {
  vault: Address<TAccountVault>;
  policy: Address<TAccountPolicy>;
  owner: TransactionSigner<TAccountOwner>;
  newValue: SetObserveOnlyInstructionDataArgs["newValue"];
};

export function getSetObserveOnlyInstruction<
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: SetObserveOnlyInput<TAccountVault, TAccountPolicy, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): SetObserveOnlyInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountPolicy,
  TAccountOwner
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    policy: { value: input.policy ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetObserveOnlyInstructionDataEncoder().encode(
      args as SetObserveOnlyInstructionDataArgs,
    ),
    programAddress,
  } as SetObserveOnlyInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountPolicy,
    TAccountOwner
  >);
}

export type ParsedSetObserveOnlyInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    policy: TAccountMetas[1];
    owner: TAccountMetas[2];
  };
  data: SetObserveOnlyInstructionData;
};

export function parseSetObserveOnlyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetObserveOnlyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      policy: getNextAccount(),
      owner: getNextAccount(),
    },
    data: getSetObserveOnlyInstructionDataDecoder().decode(instruction.data),
  };
}
