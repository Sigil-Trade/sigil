/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Phase 8 §RP Fix-Up B (PEN-02b CRITICAL, audit 2026-05-19) — owner
 * cancelled a queued OPERATOR-class agent grant during the timelock window.
 * `cancelled_agent` echoes the agent pubkey from the closed PDA so off-chain
 * monitors can correlate the queue ↔ cancel pair without re-fetching the
 * now-closed PDA. Off-chain monitors should ALERT on this event for any
 * vault they protect — if the owner did not initiate the cancel, this is
 * a phished-key attempt to abort a legitimate grant.
 */
export type AgentGrantCancelled = {
  vault: Address;
  owner: Address;
  cancelledAgent: Address;
  timestamp: bigint;
};

export type AgentGrantCancelledArgs = {
  vault: Address;
  owner: Address;
  cancelledAgent: Address;
  timestamp: number | bigint;
};

export function getAgentGrantCancelledEncoder(): FixedSizeEncoder<AgentGrantCancelledArgs> {
  return getStructEncoder([
    ["vault", getAddressEncoder()],
    ["owner", getAddressEncoder()],
    ["cancelledAgent", getAddressEncoder()],
    ["timestamp", getI64Encoder()],
  ]);
}

export function getAgentGrantCancelledDecoder(): FixedSizeDecoder<AgentGrantCancelled> {
  return getStructDecoder([
    ["vault", getAddressDecoder()],
    ["owner", getAddressDecoder()],
    ["cancelledAgent", getAddressDecoder()],
    ["timestamp", getI64Decoder()],
  ]);
}

export function getAgentGrantCancelledCodec(): FixedSizeCodec<
  AgentGrantCancelledArgs,
  AgentGrantCancelled
> {
  return combineCodec(
    getAgentGrantCancelledEncoder(),
    getAgentGrantCancelledDecoder(),
  );
}
