/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * TA-07 (Phase 3): one entry in `PolicyConfig.destination_graylist`.
 *
 * `destination` is the wallet/PDA pubkey whose ATAs are being graylisted
 * (matches the entry in `allowed_destinations`). `unlock_unix` is the
 * Unix timestamp at which the destination becomes spendable without
 * the owner having to promote it.
 *
 * Layout: 32 + 8 = 40 bytes per entry. Bounded ≤MAX_ALLOWED_DESTINATIONS
 * (10) so the worst-case Vec contribution to PolicyConfig SIZE is
 * `4 + 40*10 = 404` bytes.
 */
export type DestinationGraylistEntry = {
  destination: Address;
  unlockUnix: bigint;
};

export type DestinationGraylistEntryArgs = {
  destination: Address;
  unlockUnix: number | bigint;
};

export function getDestinationGraylistEntryEncoder(): FixedSizeEncoder<DestinationGraylistEntryArgs> {
  return getStructEncoder([
    ["destination", getAddressEncoder()],
    ["unlockUnix", getI64Encoder()],
  ]);
}

export function getDestinationGraylistEntryDecoder(): FixedSizeDecoder<DestinationGraylistEntry> {
  return getStructDecoder([
    ["destination", getAddressDecoder()],
    ["unlockUnix", getI64Decoder()],
  ]);
}

export function getDestinationGraylistEntryCodec(): FixedSizeCodec<
  DestinationGraylistEntryArgs,
  DestinationGraylistEntry
> {
  return combineCodec(
    getDestinationGraylistEntryEncoder(),
    getDestinationGraylistEntryDecoder(),
  );
}
