/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/**
 * Post-execution assertion: checks account data bytes AFTER the DeFi
 * instruction executes, within the same atomic transaction.
 *
 * Same bytes-at-offset pattern as DataConstraintZC, but applied to
 * account data instead of instruction data. Protocol-agnostic — the
 * vault owner configures byte offsets from protocol documentation.
 *
 * Phase B1: absolute value assertions (check field ≤ max, field ≥ min).
 * Phase B3 will add CrossFieldLte for leverage ratio enforcement.
 */
export type PostAssertionEntryZC = {
  /**
   * The account to read after execution (passed via remaining_accounts).
   * Typically a Position PDA, User account, or similar protocol state.
   */
  targetAccount: ReadonlyUint8Array;
  /** Byte offset in the target account's data to read. */
  offset: number;
  /** Length of the value to compare (1-32 bytes). */
  valueLen: number;
  /** Comparison operator (reuses ConstraintOperator: Eq, Ne, Gte, Lte, etc.) */
  operator: number;
  /** Expected value for comparison (same max as DataConstraint). */
  expectedValue: ReadonlyUint8Array;
  /**
   * Assertion mode:
   * 0 = Absolute: check current value against expected_value
   * 1 = MaxDecrease: check (snapshot - current) ≤ expected_value (Phase B2)
   * 2 = MaxIncrease: check (current - snapshot) ≤ expected_value (Phase B2)
   * 3 = NoChange: check current == snapshot (Phase B2)
   */
  assertionMode: number;
  /**
   * Padding to align to 8 bytes. Total: 32 + 2 + 1 + 1 + 32 + 1 + 7 = 76
   * Future: 4 bytes for cross-field offset_b (Phase B3 CrossFieldLte)
   * Future: 2 bytes for cross-field multiplier (Phase B3)
   * Future: 1 byte for cross-field flags
   */
  padding: ReadonlyUint8Array;
};

export type PostAssertionEntryZCArgs = PostAssertionEntryZC;

export function getPostAssertionEntryZCEncoder(): FixedSizeEncoder<PostAssertionEntryZCArgs> {
  return getStructEncoder([
    ["targetAccount", fixEncoderSize(getBytesEncoder(), 32)],
    ["offset", getU16Encoder()],
    ["valueLen", getU8Encoder()],
    ["operator", getU8Encoder()],
    ["expectedValue", fixEncoderSize(getBytesEncoder(), 32)],
    ["assertionMode", getU8Encoder()],
    ["padding", fixEncoderSize(getBytesEncoder(), 7)],
  ]);
}

export function getPostAssertionEntryZCDecoder(): FixedSizeDecoder<PostAssertionEntryZC> {
  return getStructDecoder([
    ["targetAccount", fixDecoderSize(getBytesDecoder(), 32)],
    ["offset", getU16Decoder()],
    ["valueLen", getU8Decoder()],
    ["operator", getU8Decoder()],
    ["expectedValue", fixDecoderSize(getBytesDecoder(), 32)],
    ["assertionMode", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 7)],
  ]);
}

export function getPostAssertionEntryZCCodec(): FixedSizeCodec<
  PostAssertionEntryZCArgs,
  PostAssertionEntryZC
> {
  return combineCodec(
    getPostAssertionEntryZCEncoder(),
    getPostAssertionEntryZCDecoder(),
  );
}
