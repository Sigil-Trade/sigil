/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Phase 8 PEN-CROSS-1 — owner queued an OPERATOR-class agent grant. The
 * agent is NOT yet in `vault.agents`; off-chain monitors should ALERT on
 * this event for any vault they protect. If the owner didn't initiate the
 * queue, this is a phished-key attack signal — the owner has
 * `min_delay_seconds` (default 1800s = 30 min) to abort before
 * `apply_agent_grant` can land. (A `cancel_agent_grant` instruction is
 * planned for a follow-up batch; until then, observers should freeze the
 * vault if the queue was unauthorized.)
 */
export type AgentGrantQueued = {
  vault: Address;
  agent: Address;
  capability: number;
  spendingLimitUsd: bigint;
  queuedAt: bigint;
  executesAt: bigint;
};

export type AgentGrantQueuedArgs = {
  vault: Address;
  agent: Address;
  capability: number;
  spendingLimitUsd: number | bigint;
  queuedAt: number | bigint;
  executesAt: number | bigint;
};

export function getAgentGrantQueuedEncoder(): FixedSizeEncoder<AgentGrantQueuedArgs> {
  return getStructEncoder([
    ["vault", getAddressEncoder()],
    ["agent", getAddressEncoder()],
    ["capability", getU8Encoder()],
    ["spendingLimitUsd", getU64Encoder()],
    ["queuedAt", getI64Encoder()],
    ["executesAt", getI64Encoder()],
  ]);
}

export function getAgentGrantQueuedDecoder(): FixedSizeDecoder<AgentGrantQueued> {
  return getStructDecoder([
    ["vault", getAddressDecoder()],
    ["agent", getAddressDecoder()],
    ["capability", getU8Decoder()],
    ["spendingLimitUsd", getU64Decoder()],
    ["queuedAt", getI64Decoder()],
    ["executesAt", getI64Decoder()],
  ]);
}

export function getAgentGrantQueuedCodec(): FixedSizeCodec<
  AgentGrantQueuedArgs,
  AgentGrantQueued
> {
  return combineCodec(
    getAgentGrantQueuedEncoder(),
    getAgentGrantQueuedDecoder(),
  );
}
