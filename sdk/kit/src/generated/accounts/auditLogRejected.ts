/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAuditEntryDecoder,
  getAuditEntryEncoder,
  type AuditEntry,
  type AuditEntryArgs,
} from "../types/index.js";

export const AUDIT_LOG_REJECTED_DISCRIMINATOR = new Uint8Array([
  211, 117, 26, 31, 160, 74, 242, 204,
]);

export function getAuditLogRejectedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    AUDIT_LOG_REJECTED_DISCRIMINATOR,
  );
}

export type AuditLogRejected = {
  discriminator: ReadonlyUint8Array;
  /**
   * Associated vault pubkey. Verified by PDA seeds + has_one constraints
   * at the instruction layer.
   */
  vault: Address;
  /** Circular-buffer entries. Same shape as success buffer. */
  entries: Array<AuditEntry>;
  /** Next write position (0..=CAPACITY-1). Wraps modulo CAPACITY. */
  head: number;
  /** Total entries written, saturated at CAPACITY. */
  count: number;
  /**
   * 13-byte explicit padding (Pod no-implicit-padding rule + future
   * forward-compat appends).
   */
  padding: ReadonlyUint8Array;
  /** PDA bump seed. */
  bump: number;
};

export type AuditLogRejectedArgs = {
  /**
   * Associated vault pubkey. Verified by PDA seeds + has_one constraints
   * at the instruction layer.
   */
  vault: Address;
  /** Circular-buffer entries. Same shape as success buffer. */
  entries: Array<AuditEntryArgs>;
  /** Next write position (0..=CAPACITY-1). Wraps modulo CAPACITY. */
  head: number;
  /** Total entries written, saturated at CAPACITY. */
  count: number;
  /**
   * 13-byte explicit padding (Pod no-implicit-padding rule + future
   * forward-compat appends).
   */
  padding: ReadonlyUint8Array;
  /** PDA bump seed. */
  bump: number;
};

/** Gets the encoder for {@link AuditLogRejectedArgs} account data. */
export function getAuditLogRejectedEncoder(): FixedSizeEncoder<AuditLogRejectedArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vault", getAddressEncoder()],
      ["entries", getArrayEncoder(getAuditEntryEncoder(), { size: 64 })],
      ["head", getU8Encoder()],
      ["count", getU8Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 13)],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: AUDIT_LOG_REJECTED_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link AuditLogRejected} account data. */
export function getAuditLogRejectedDecoder(): FixedSizeDecoder<AuditLogRejected> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vault", getAddressDecoder()],
    ["entries", getArrayDecoder(getAuditEntryDecoder(), { size: 64 })],
    ["head", getU8Decoder()],
    ["count", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 13)],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link AuditLogRejected} account data. */
export function getAuditLogRejectedCodec(): FixedSizeCodec<
  AuditLogRejectedArgs,
  AuditLogRejected
> {
  return combineCodec(
    getAuditLogRejectedEncoder(),
    getAuditLogRejectedDecoder(),
  );
}

export function decodeAuditLogRejected<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<AuditLogRejected, TAddress>;
export function decodeAuditLogRejected<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<AuditLogRejected, TAddress>;
export function decodeAuditLogRejected<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<AuditLogRejected, TAddress>
  | MaybeAccount<AuditLogRejected, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAuditLogRejectedDecoder(),
  );
}

export async function fetchAuditLogRejected<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<AuditLogRejected, TAddress>> {
  const maybeAccount = await fetchMaybeAuditLogRejected(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAuditLogRejected<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<AuditLogRejected, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAuditLogRejected(maybeAccount);
}

export async function fetchAllAuditLogRejected(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<AuditLogRejected>[]> {
  const maybeAccounts = await fetchAllMaybeAuditLogRejected(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAuditLogRejected(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<AuditLogRejected>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeAuditLogRejected(maybeAccount),
  );
}

export function getAuditLogRejectedSize(): number {
  return 4152;
}
