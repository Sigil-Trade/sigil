/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const PENDING_OWNERSHIP_TRANSFER_DISCRIMINATOR = new Uint8Array([
  205, 223, 35, 217, 245, 217, 152, 38,
]);

export function getPendingOwnershipTransferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PENDING_OWNERSHIP_TRANSFER_DISCRIMINATOR,
  );
}

export type PendingOwnershipTransfer = {
  discriminator: ReadonlyUint8Array;
  /**
   * PDA-bound vault. Defense-in-depth duplicate of the [b"pending_owner",
   * vault.key()] seed — also lets handlers reject stale accounts that were
   * re-created against a different vault during a close-then-reuse race.
   */
  vault: Address;
  /**
   * Owner pubkey at queue time. `cancel_ownership_transfer` requires the
   * signer match this field exactly (in addition to `has_one = owner` on
   * the vault), and the PDA's rent reverts here on cancel.
   */
  currentOwner: Address;
  /**
   * Target owner. `accept_ownership_transfer` requires the signer match
   * this field exactly (standard EOA path). Multisig variant in Batch 4
   * will also bind here when `is_multisig_target == true`.
   */
  newOwner: Address;
  /**
   * `Clock::unix_timestamp` at queue time. Timelock is enforced as
   * `clock.unix_timestamp - queued_at >= min_delay_seconds`.
   */
  queuedAt: bigint;
  /**
   * Owner-configurable timelock (seconds). Defaults to
   * `Self::DEFAULT_MIN_DELAY` (172,800 / 48h). Owner can shorten in a
   * future SDK call if `policy.timelock_duration` permits, but Batch 3
   * pins the default — extension hook lives in Batch 4+.
   */
  minDelaySeconds: bigint;
  /**
   * `true` means the accept path will be `accept_ownership_transfer_multisig`
   * (Batch 4 — Squads V4 vault-PDA-signs flow). `false` means the standard
   * EOA accept path. Today's `accept_ownership_transfer` HARD-REJECTS when
   * this is `true` so the multisig flow cannot be silently taken by the
   * regular handler before Batch 4 ships.
   */
  isMultisigTarget: boolean;
  /** PDA bump. */
  bump: number;
  /**
   * 8-byte alignment cushion + additive headroom for Batch 4+ extensions
   * (e.g. cooldown packing, multisig-attestation digest). Zero-init on
   * `init` and unread today.
   */
  padding: ReadonlyUint8Array;
  /**
   * CH-1 close (Bucket-3 audit 2026-05-23): slot at queue time for
   * F-10 freshness. See pending_agent_grant.rs for the threat model.
   */
  queuedAtSlot: bigint;
};

export type PendingOwnershipTransferArgs = {
  /**
   * PDA-bound vault. Defense-in-depth duplicate of the [b"pending_owner",
   * vault.key()] seed — also lets handlers reject stale accounts that were
   * re-created against a different vault during a close-then-reuse race.
   */
  vault: Address;
  /**
   * Owner pubkey at queue time. `cancel_ownership_transfer` requires the
   * signer match this field exactly (in addition to `has_one = owner` on
   * the vault), and the PDA's rent reverts here on cancel.
   */
  currentOwner: Address;
  /**
   * Target owner. `accept_ownership_transfer` requires the signer match
   * this field exactly (standard EOA path). Multisig variant in Batch 4
   * will also bind here when `is_multisig_target == true`.
   */
  newOwner: Address;
  /**
   * `Clock::unix_timestamp` at queue time. Timelock is enforced as
   * `clock.unix_timestamp - queued_at >= min_delay_seconds`.
   */
  queuedAt: number | bigint;
  /**
   * Owner-configurable timelock (seconds). Defaults to
   * `Self::DEFAULT_MIN_DELAY` (172,800 / 48h). Owner can shorten in a
   * future SDK call if `policy.timelock_duration` permits, but Batch 3
   * pins the default — extension hook lives in Batch 4+.
   */
  minDelaySeconds: number | bigint;
  /**
   * `true` means the accept path will be `accept_ownership_transfer_multisig`
   * (Batch 4 — Squads V4 vault-PDA-signs flow). `false` means the standard
   * EOA accept path. Today's `accept_ownership_transfer` HARD-REJECTS when
   * this is `true` so the multisig flow cannot be silently taken by the
   * regular handler before Batch 4 ships.
   */
  isMultisigTarget: boolean;
  /** PDA bump. */
  bump: number;
  /**
   * 8-byte alignment cushion + additive headroom for Batch 4+ extensions
   * (e.g. cooldown packing, multisig-attestation digest). Zero-init on
   * `init` and unread today.
   */
  padding: ReadonlyUint8Array;
  /**
   * CH-1 close (Bucket-3 audit 2026-05-23): slot at queue time for
   * F-10 freshness. See pending_agent_grant.rs for the threat model.
   */
  queuedAtSlot: number | bigint;
};

/** Gets the encoder for {@link PendingOwnershipTransferArgs} account data. */
export function getPendingOwnershipTransferEncoder(): FixedSizeEncoder<PendingOwnershipTransferArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vault", getAddressEncoder()],
      ["currentOwner", getAddressEncoder()],
      ["newOwner", getAddressEncoder()],
      ["queuedAt", getI64Encoder()],
      ["minDelaySeconds", getU64Encoder()],
      ["isMultisigTarget", getBooleanEncoder()],
      ["bump", getU8Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 6)],
      ["queuedAtSlot", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PENDING_OWNERSHIP_TRANSFER_DISCRIMINATOR,
    }),
  );
}

/** Gets the decoder for {@link PendingOwnershipTransfer} account data. */
export function getPendingOwnershipTransferDecoder(): FixedSizeDecoder<PendingOwnershipTransfer> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vault", getAddressDecoder()],
    ["currentOwner", getAddressDecoder()],
    ["newOwner", getAddressDecoder()],
    ["queuedAt", getI64Decoder()],
    ["minDelaySeconds", getU64Decoder()],
    ["isMultisigTarget", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 6)],
    ["queuedAtSlot", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link PendingOwnershipTransfer} account data. */
export function getPendingOwnershipTransferCodec(): FixedSizeCodec<
  PendingOwnershipTransferArgs,
  PendingOwnershipTransfer
> {
  return combineCodec(
    getPendingOwnershipTransferEncoder(),
    getPendingOwnershipTransferDecoder(),
  );
}

export function decodePendingOwnershipTransfer<
  TAddress extends string = string,
>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<PendingOwnershipTransfer, TAddress>;
export function decodePendingOwnershipTransfer<
  TAddress extends string = string,
>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<PendingOwnershipTransfer, TAddress>;
export function decodePendingOwnershipTransfer<
  TAddress extends string = string,
>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<PendingOwnershipTransfer, TAddress>
  | MaybeAccount<PendingOwnershipTransfer, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPendingOwnershipTransferDecoder(),
  );
}

export async function fetchPendingOwnershipTransfer<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PendingOwnershipTransfer, TAddress>> {
  const maybeAccount = await fetchMaybePendingOwnershipTransfer(
    rpc,
    address,
    config,
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePendingOwnershipTransfer<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PendingOwnershipTransfer, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePendingOwnershipTransfer(maybeAccount);
}

export async function fetchAllPendingOwnershipTransfer(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PendingOwnershipTransfer>[]> {
  const maybeAccounts = await fetchAllMaybePendingOwnershipTransfer(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePendingOwnershipTransfer(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PendingOwnershipTransfer>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodePendingOwnershipTransfer(maybeAccount),
  );
}

export function getPendingOwnershipTransferSize(): number {
  return 136;
}
