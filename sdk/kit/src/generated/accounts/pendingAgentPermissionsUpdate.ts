/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const PENDING_AGENT_PERMISSIONS_UPDATE_DISCRIMINATOR = new Uint8Array([
  137, 132, 60, 184, 171, 184, 194, 56,
]);

export function getPendingAgentPermissionsUpdateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PENDING_AGENT_PERMISSIONS_UPDATE_DISCRIMINATOR,
  );
}

export type PendingAgentPermissionsUpdate = {
  discriminator: ReadonlyUint8Array;
  vault: Address;
  agent: Address;
  newCapability: number;
  reservedCap: ReadonlyUint8Array;
  spendingLimitUsd: bigint;
  queuedAt: bigint;
  executesAt: bigint;
  /**
   * Slot number when this update was queued. Paired with `MAX_APPLY_AGE_SLOTS`
   * to enforce a freshness ceiling — defends against durable-nonce pre-signing
   * attacks (F-10 audit fix, Drift Protocol April 2026 $285M analog).
   */
  queuedAtSlot: bigint;
  bump: number;
  /**
   * TA-06 (Phase 3): per-agent cooldown in seconds. 0 disables. Bound
   * at apply time onto `AgentSpendOverlay.cooldown_seconds[slot]`.
   * APPENDED at end per F-14 APPEND-ONLY rule for Borsh stability.
   */
  cooldownSeconds: bigint;
  /**
   * Round 2 F-RP3-2 fix (audit 2026-05-19): cosign-binding digest for
   * elevated mutations. When `queue_agent_permissions_update` detects
   * that the request RAISES an agent's capability, RAISES the spending
   * limit, or SHORTENS the cooldown — AND `policy.cosign_required ==
   * true` — the owner MUST supply a co-signing session in the accounts.
   * The queue handler computes a sha256 over the canonical pending args
   * and stores it here; `apply_agent_permissions_update` re-asserts the
   * digest equality.
   *
   * `[0u8; 32]` = no cosign required (non-elevated mutation OR cosign
   * not opted in on this vault). Any non-zero digest indicates this
   * pending was bound to a specific cosign and the apply handler MUST
   * re-compute and equal-check.
   *
   * APPENDED at end per F-14 APPEND-ONLY rule for Borsh stability.
   */
  cosignDigest: ReadonlyUint8Array;
  /**
   * Round 2 F-RP3-2 fix (audit 2026-05-19): pubkey of the session that
   * co-signed this queue. Recorded for audit. `Pubkey::default()` =
   * no cosign (non-elevated OR not opted in).
   *
   * APPENDED at end per F-14 APPEND-ONLY rule for Borsh stability.
   */
  cosignSession: Address;
};

export type PendingAgentPermissionsUpdateArgs = {
  vault: Address;
  agent: Address;
  newCapability: number;
  reservedCap: ReadonlyUint8Array;
  spendingLimitUsd: number | bigint;
  queuedAt: number | bigint;
  executesAt: number | bigint;
  /**
   * Slot number when this update was queued. Paired with `MAX_APPLY_AGE_SLOTS`
   * to enforce a freshness ceiling — defends against durable-nonce pre-signing
   * attacks (F-10 audit fix, Drift Protocol April 2026 $285M analog).
   */
  queuedAtSlot: number | bigint;
  bump: number;
  /**
   * TA-06 (Phase 3): per-agent cooldown in seconds. 0 disables. Bound
   * at apply time onto `AgentSpendOverlay.cooldown_seconds[slot]`.
   * APPENDED at end per F-14 APPEND-ONLY rule for Borsh stability.
   */
  cooldownSeconds: number | bigint;
  /**
   * Round 2 F-RP3-2 fix (audit 2026-05-19): cosign-binding digest for
   * elevated mutations. When `queue_agent_permissions_update` detects
   * that the request RAISES an agent's capability, RAISES the spending
   * limit, or SHORTENS the cooldown — AND `policy.cosign_required ==
   * true` — the owner MUST supply a co-signing session in the accounts.
   * The queue handler computes a sha256 over the canonical pending args
   * and stores it here; `apply_agent_permissions_update` re-asserts the
   * digest equality.
   *
   * `[0u8; 32]` = no cosign required (non-elevated mutation OR cosign
   * not opted in on this vault). Any non-zero digest indicates this
   * pending was bound to a specific cosign and the apply handler MUST
   * re-compute and equal-check.
   *
   * APPENDED at end per F-14 APPEND-ONLY rule for Borsh stability.
   */
  cosignDigest: ReadonlyUint8Array;
  /**
   * Round 2 F-RP3-2 fix (audit 2026-05-19): pubkey of the session that
   * co-signed this queue. Recorded for audit. `Pubkey::default()` =
   * no cosign (non-elevated OR not opted in).
   *
   * APPENDED at end per F-14 APPEND-ONLY rule for Borsh stability.
   */
  cosignSession: Address;
};

/** Gets the encoder for {@link PendingAgentPermissionsUpdateArgs} account data. */
export function getPendingAgentPermissionsUpdateEncoder(): FixedSizeEncoder<PendingAgentPermissionsUpdateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vault", getAddressEncoder()],
      ["agent", getAddressEncoder()],
      ["newCapability", getU8Encoder()],
      ["reservedCap", fixEncoderSize(getBytesEncoder(), 7)],
      ["spendingLimitUsd", getU64Encoder()],
      ["queuedAt", getI64Encoder()],
      ["executesAt", getI64Encoder()],
      ["queuedAtSlot", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["cooldownSeconds", getU64Encoder()],
      ["cosignDigest", fixEncoderSize(getBytesEncoder(), 32)],
      ["cosignSession", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PENDING_AGENT_PERMISSIONS_UPDATE_DISCRIMINATOR,
    }),
  );
}

/** Gets the decoder for {@link PendingAgentPermissionsUpdate} account data. */
export function getPendingAgentPermissionsUpdateDecoder(): FixedSizeDecoder<PendingAgentPermissionsUpdate> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vault", getAddressDecoder()],
    ["agent", getAddressDecoder()],
    ["newCapability", getU8Decoder()],
    ["reservedCap", fixDecoderSize(getBytesDecoder(), 7)],
    ["spendingLimitUsd", getU64Decoder()],
    ["queuedAt", getI64Decoder()],
    ["executesAt", getI64Decoder()],
    ["queuedAtSlot", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["cooldownSeconds", getU64Decoder()],
    ["cosignDigest", fixDecoderSize(getBytesDecoder(), 32)],
    ["cosignSession", getAddressDecoder()],
  ]);
}

/** Gets the codec for {@link PendingAgentPermissionsUpdate} account data. */
export function getPendingAgentPermissionsUpdateCodec(): FixedSizeCodec<
  PendingAgentPermissionsUpdateArgs,
  PendingAgentPermissionsUpdate
> {
  return combineCodec(
    getPendingAgentPermissionsUpdateEncoder(),
    getPendingAgentPermissionsUpdateDecoder(),
  );
}

export function decodePendingAgentPermissionsUpdate<
  TAddress extends string = string,
>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<PendingAgentPermissionsUpdate, TAddress>;
export function decodePendingAgentPermissionsUpdate<
  TAddress extends string = string,
>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<PendingAgentPermissionsUpdate, TAddress>;
export function decodePendingAgentPermissionsUpdate<
  TAddress extends string = string,
>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<PendingAgentPermissionsUpdate, TAddress>
  | MaybeAccount<PendingAgentPermissionsUpdate, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPendingAgentPermissionsUpdateDecoder(),
  );
}

export async function fetchPendingAgentPermissionsUpdate<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PendingAgentPermissionsUpdate, TAddress>> {
  const maybeAccount = await fetchMaybePendingAgentPermissionsUpdate(
    rpc,
    address,
    config,
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePendingAgentPermissionsUpdate<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PendingAgentPermissionsUpdate, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePendingAgentPermissionsUpdate(maybeAccount);
}

export async function fetchAllPendingAgentPermissionsUpdate(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PendingAgentPermissionsUpdate>[]> {
  const maybeAccounts = await fetchAllMaybePendingAgentPermissionsUpdate(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePendingAgentPermissionsUpdate(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PendingAgentPermissionsUpdate>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodePendingAgentPermissionsUpdate(maybeAccount),
  );
}

export function getPendingAgentPermissionsUpdateSize(): number {
  return 185;
}
