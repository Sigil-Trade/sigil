/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAuditEntryDecoder,
  getAuditEntryEncoder,
  type AuditEntry,
  type AuditEntryArgs,
} from "../types/index.js";

export const AUDIT_LOG_SUCCESS_DISCRIMINATOR = new Uint8Array([
  225, 112, 129, 30, 0, 111, 84, 75,
]);

export function getAuditLogSuccessDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    AUDIT_LOG_SUCCESS_DISCRIMINATOR,
  );
}

export type AuditLogSuccess = {
  discriminator: ReadonlyUint8Array;
  /**
   * Associated vault pubkey. Verified by PDA seeds + has_one constraints
   * at the instruction layer.
   */
  vault: Address;
  /**
   * Circular-buffer entries. Indexed by `(head - 1) mod CAPACITY` for the
   * most recent entry; oldest is at `head` when `count == CAPACITY`.
   */
  entries: Array<AuditEntry>;
  /** Next write position (0..=CAPACITY-1). Wraps modulo CAPACITY. */
  head: number;
  /**
   * Total entries written, saturated at CAPACITY. Used by readers to
   * distinguish a half-filled buffer from a wrapped one.
   */
  count: number;
  /**
   * 13-byte explicit padding to satisfy Pod's no-implicit-padding rule.
   * Forward-compat slot for future field appends (see Phase 9+).
   */
  padding: ReadonlyUint8Array;
  /** PDA bump seed. */
  bump: number;
};

export type AuditLogSuccessArgs = {
  /**
   * Associated vault pubkey. Verified by PDA seeds + has_one constraints
   * at the instruction layer.
   */
  vault: Address;
  /**
   * Circular-buffer entries. Indexed by `(head - 1) mod CAPACITY` for the
   * most recent entry; oldest is at `head` when `count == CAPACITY`.
   */
  entries: Array<AuditEntryArgs>;
  /** Next write position (0..=CAPACITY-1). Wraps modulo CAPACITY. */
  head: number;
  /**
   * Total entries written, saturated at CAPACITY. Used by readers to
   * distinguish a half-filled buffer from a wrapped one.
   */
  count: number;
  /**
   * 13-byte explicit padding to satisfy Pod's no-implicit-padding rule.
   * Forward-compat slot for future field appends (see Phase 9+).
   */
  padding: ReadonlyUint8Array;
  /** PDA bump seed. */
  bump: number;
};

/** Gets the encoder for {@link AuditLogSuccessArgs} account data. */
export function getAuditLogSuccessEncoder(): FixedSizeEncoder<AuditLogSuccessArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vault", getAddressEncoder()],
      ["entries", getArrayEncoder(getAuditEntryEncoder(), { size: 128 })],
      ["head", getU8Encoder()],
      ["count", getU8Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 13)],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: AUDIT_LOG_SUCCESS_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link AuditLogSuccess} account data. */
export function getAuditLogSuccessDecoder(): FixedSizeDecoder<AuditLogSuccess> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vault", getAddressDecoder()],
    ["entries", getArrayDecoder(getAuditEntryDecoder(), { size: 128 })],
    ["head", getU8Decoder()],
    ["count", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 13)],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link AuditLogSuccess} account data. */
export function getAuditLogSuccessCodec(): FixedSizeCodec<
  AuditLogSuccessArgs,
  AuditLogSuccess
> {
  return combineCodec(getAuditLogSuccessEncoder(), getAuditLogSuccessDecoder());
}

export function decodeAuditLogSuccess<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<AuditLogSuccess, TAddress>;
export function decodeAuditLogSuccess<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<AuditLogSuccess, TAddress>;
export function decodeAuditLogSuccess<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<AuditLogSuccess, TAddress>
  | MaybeAccount<AuditLogSuccess, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAuditLogSuccessDecoder(),
  );
}

export async function fetchAuditLogSuccess<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<AuditLogSuccess, TAddress>> {
  const maybeAccount = await fetchMaybeAuditLogSuccess(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAuditLogSuccess<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<AuditLogSuccess, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAuditLogSuccess(maybeAccount);
}

export async function fetchAllAuditLogSuccess(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<AuditLogSuccess>[]> {
  const maybeAccounts = await fetchAllMaybeAuditLogSuccess(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAuditLogSuccess(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<AuditLogSuccess>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeAuditLogSuccess(maybeAccount),
  );
}

export function getAuditLogSuccessSize(): number {
  return 8248;
}
